# FirebaseLogout

> Logs user out of Firebase.

```js
import { FirebaseLogout } from 'refire-app'
const LogoutButton = () => {
  return (
    <FirebaseLogout>
      <Button>Logout</Button>
    </FirebaseLogout>
  )
}
```
